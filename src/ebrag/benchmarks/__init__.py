"""Benchmark harness — minimal slice used by the C4 VLDB submission artefacts.

Most of the original ebrag benchmarks package (full runner, metrics, datasets) is
intentionally omitted from this artefact; only the C4 experiments are kept.
"""
