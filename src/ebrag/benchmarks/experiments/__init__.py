"""Experiment drivers.

Each module runs one experiment end-to-end and emits machine-readable results (JSON).
The renderer in ``c4_render_tables.py`` turns those into LaTeX table fragments for the
paper.
"""
